# Backend Environment Variables (example)
NODE_ENV=development
PORT=4000

# Option A: explicit connection params
DB_HOST=db
DB_PORT=5432
DB_USER=foodapp
DB_PASS=foodapp_password
DB_NAME=foodapp

# Option B: single connection string (preferred in cloud). If set, overrides DB_* above.
# DATABASE_URL=postgres://user:pass@host:5432/dbname

JWT_SECRET=super_secret_jwt_key
CORS_ORIGIN=http://localhost:3000
SEED_ON_START=true